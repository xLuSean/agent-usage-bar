<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>Agent Usage Bar</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- Set by scripts/package_dmg.sh. Empty for builds run straight from Xcode,
	     which is how the About panel tells a packaged build from a local one. -->
	<key>AUBSourceCommit</key>
	<string>$(AUB_SOURCE_COMMIT)</string>
	<!-- Committed release-channel metadata. It is read back from mounted DMGs so an
	     alpha candidate cannot be mislabeled as a final release. -->
	<key>AUBReleaseSuffix</key>
	<string>$(AUB_RELEASE_SUFFIX)</string>
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.utilities</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<!-- Launches as a menu bar utility with no Dock icon. Opening Settings switches the
	     activation policy to .regular at runtime so the window gets a Dock icon, and
	     closing it switches back. -->
	<key>LSUIElement</key>
	<true/>
	<key>NSHumanReadableCopyright</key>
	<string>Not affiliated with Anthropic or OpenAI. All artwork is original.</string>
</dict>
</plist>
